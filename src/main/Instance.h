#include <string>

class Instance {
private:
    int size;
    int * sol;
    int * edges [];

public:
    Instance(int _size, int * _sol): size(_size), sol(_sol){}

    void permute(){

    }
    ~Instance(){

    }
};
